package javastarter.homework.tasks.task06;

public class Count {

    public static void countsOfDigits(int num) {
        int numDigits = String.valueOf(num).length();
        System.out.println(numDigits);
    }
}